# Unhobble Audit — 貼進 Claude Code 就跑

複製底下整段(從 `---` 開始到結尾),貼進你的 Claude Code。不用安裝任何東西。

---

你是我的 Claude Code 設定稽核員。請對我這台機器做一次完整的 harness 稽核,產出報告。

## 邊界(先讀,不可違反)

- **全程唯讀。**不刪除、不修改、不移動我的任何設定檔。
- **不外傳。**所有讀到的內容只在本機分析,不上傳、不寫進任何遠端。
- **最後只出建議清單,刪除動作由我自己執行。**

## 背景:為什麼要做這件事

Claude Code 作者 Boris Cherny 在 2026-07-27 的 Y Combinator 對談中提到,他們每次新模型發布都會刪掉大部分 system prompt——Opus 5 那次砍掉 80%。理由是:大量指令是「修正模型本來不會、但現在已經會了的行為」的補丁,新模型不需要,留著只是每輪都在燒 context。他給使用者的建議是每半年刪掉自己的 CLAUDE.md、skills、hooks,重新用「刪 → 用 → 只有重複卡在同一件事才加回」的方式重建。

這份稽核把那個建議變成可執行的量測。**先給我數字,再給我意見。**

## Phase 1 — 靜態足跡(免費,先做)

用 bash 量,全部要同時看使用者層(`~/.claude/`)和專案層(`./.claude/`),沒有的目錄要 graceful 跳過:

- `CLAUDE.md` / `AGENTS.md` 各層的 bytes 與行數
- skill 數量,以及**所有 skill description 的合計 bytes**(這是每個 session 的固定租金——skill 的 description 不管有沒有觸發都常駐在 context)
- memory 檔數與索引檔大小(路徑含專案 slug,要自己找,很多人根本沒有這個目錄)
- hook 檔數
- MCP server 數與工具總數

## Phase 2 — 真實 token 成本(要花我一次 API 額度,先問我)

**先問我要不要跑。**我同意才執行:

```bash
claude -p "Reply with exactly: ok" --output-format json
```

把回傳 `usage` 裡的 `input_tokens + cache_creation_input_tokens + cache_read_input_tokens` 加總——這是我問一句廢話就要付的常駐成本。**跑三次取中位數**,單次有 ±2% 抖動。

若回傳 `Not logged in`,那是執行環境的憑證隔離,不是我沒登入。請把指令給我,讓我自己在終端機跑,不要把它當成 0。

## Phase 3 — 殭屍 skill 偵測(免費,這是重點)

掃我的對話記錄,統計每個 skill 實際被觸發過幾次:

**skill 有兩條完全不同的觸發路徑,只掃一條會產生大量假殭屍。**必須兩條都掃:

```bash
# 來源一:模型讀 description 自動觸發(記成 Skill 工具呼叫)
grep -rho '"name":"Skill","input":{[^}]*}' ~/.claude/projects --include='*.jsonl' 2>/dev/null \
  | grep -o '"skill":"[^"]*"' | sed 's/.*:"//;s/"//;s/.*://' | sort | uniq -c

# 來源二:使用者手打 /名稱(記成 command-name,完全不同的格式)
grep -rho '<command-name>/[a-z0-9:_-]*</command-name>' ~/.claude/projects --include='*.jsonl' 2>/dev/null \
  | sed 's/<[^>]*>//g;s|^/||;s/.*://' | sort | uniq -c
```

把兩份數字相加,再跟 `~/.claude/skills/` 實際安裝清單交叉比對,**兩邊都是 0 的才算殭屍**。

作者在自己機器上第一版只掃來源一,得到「20/34 從未觸發」;補上來源二之後真實數字是 16/34——其中一個被誤判的 skill 實際上被手動叫過 37 次。只掃一條路徑會害人刪掉天天在用的東西。

**已知剩餘盲區(要在報告裡誠實標註)**:被 cron / 排程 / 外部腳本驅動的 skill,可能兩條路徑都不留痕跡。次數 0 只代表「這兩種入口沒看到」,不等於沒用——出報告時要把 0 次的項目標成「待確認」而不是「確定可刪」。

實務注意:記錄可能上千個檔案,grep 要給 timeout;plugin skill 名稱是 `plugin:skill` 格式,尾端 `sed` 負責正規化。

## Phase 4 — 規則年齡

用 `git log` 或檔案 mtime,標出每條規則、每個 skill 最後一次修改是什麼時候。把時間換算成「當時是哪一代模型」。**寫在三個月前、之後沒動過的規則,是最可疑的補丁候選。**

## Phase 5 — 分類(核心,只有你做得到)

把每一條規則、每一個 skill 判進兩籃。**這個分野搞錯會出事:**

**A. 模型補丁(可刪)** — 為了繞舊模型能力不足而寫的。特徵:
- 「不要自己臆測」「一定要先用 X 工具查」「你無法自己完成這件事」
- 大段的觸發條件窮舉與反例列表(過度規格化)
- 教模型怎麼一步一步做,而不是描述目標與完成標準
- 功能重疊的同類 skill(同一件事有三四個入口)

**B. 行為偏好(必須留)** — 跟模型多聰明無關,是我要的工作方式:
- 輸出語言、格式、回報節奏
- 安全閘門、破壞性操作確認、credential 保護
- 專案領域詞彙、命名慣例
- 我明確講過的偏好

**不確定的一律歸 B。**寧可留著也不要害我刪掉安全機制。

## Phase 6 — 補上大多數人缺的兩塊

Boris 說 verification 是最多人做不好的單一件事——不是 prompt engineering。掃一下我有沒有:

1. **內建驗證機制**:我丟給 agent 的任務,有沒有讓它能自己判斷「做完了沒、做對了沒」的東西(測試套件、截圖比對、lint、健檢端點)。沒有驗證的任務跑不久,因為模型會卡住或亂編。
2. **自動化維護迴圈**:有沒有任何排程在幫我清死碼、合併重複抽象、補測試覆蓋。多數人這格是空的。

## Phase 7 — 產出

給我三份東西:

**① 完整報告**(存成本機 markdown,含規則內容)

**② 可分享的數字卡**——只有數字、**不含任何規則內容或路徑**,例如:
```
常駐 overhead: 54,042 tokens/turn
Skills: 34 個,16 個從未觸發 (47%)
Skill descriptions: 46 KB 常駐
Memory: 96 檔 / 索引 20 KB
```
這張卡我要能直接貼出去,所以絕對不能含我的專案名、路徑或規則內容。

**③ Ablation 執行計畫**,按這個順序:
1. **第一步強制備份**:`cp -r ~/.claude ~/.claude.bak.$(date +%F)`
2. 刪除候選清單,按「省下的 token × 風險低」排序,每項附一句為什麼判成補丁
3. 提醒我正確流程是 **刪 → 實際用一陣子 → 只有重複卡在同一件事才把那條加回來**,不要一次改完就當結束

**你不要幫我刪任何東西。**出清單就好。

---

來源:Boris Cherny @ Y Combinator, 2026-07-27, https://youtu.be/qyPCVqFUyDo
